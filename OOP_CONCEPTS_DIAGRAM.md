# SMART PARKING BACKEND - OOP CONCEPTS & CLASS DIAGRAM

## 🎓 OBJECT-ORIENTED PROGRAMMING CONCEPTS IN THIS PROJECT

This document maps all OOP concepts (Abstraction, Inheritance, Polymorphism, Encapsulation) to their implementation in the Smart Parking Backend.

---

## 📊 COMPLETE ABSTRACT CLASS DIAGRAM

```
╔══════════════════════════════════════════════════════════════════╗
║                    ABSTRACTION                                    ║
║  (Interfaces define "WHAT" without "HOW")                        ║
╚══════════════════════════════════════════════════════════════════╝

┌────────────────────────────────────────────────────────────────┐
│                     <<interface>>                               │
│                     JpaRepository<T, ID>                        │
│                   (Spring Data JPA - Framework)                 │
├────────────────────────────────────────────────────────────────┤
│ + save(entity: T): T                                            │
│ + findById(id: ID): Optional<T>                                 │
│ + findAll(): List<T>                                            │
│ + deleteById(id: ID): void                                      │
│ + count(): long                                                 │
│ + existsById(id: ID): boolean                                   │
└──────────────────────┬─────────────────────────────────────────┘
                       │ extends (INHERITANCE)
         ┌─────────────┼─────────────┬──────────────┬────────────┐
         │             │             │              │            │
         ▼             ▼             ▼              ▼            ▼
┌────────────────┐ ┌────────────┐ ┌─────────────┐ ┌──────────┐ ┌──────────┐
│  <<interface>> │ │<<interface>>│ │<<interface>>│ │<<interface>>│ │<<interface>>│
│   Employee     │ │  Vehicle   │ │ ParkingSlot │ │   Floor  │ │ParkingLot│
│   Repository   │ │ Repository │ │ Repository  │ │Repository│ │Repository│
├────────────────┤ ├────────────┤ ├─────────────┤ ├──────────┤ ├──────────┤
│+ findByEmail() │ │+ findBy    │ │+ findFirst  │ │(inherits │ │(inherits │
│+ existsByEmail││ │  Vehicle   │ │  BySlotType │ │ only)    │ │ only)    │
│                │ │  Registration││  AndSlot    │ │          │ │          │
│                │ │+ findBy    │ │  Status()   │ │          │ │          │
│                │ │  Status()  │ │+ countBy... │ │          │ │          │
└────────────────┘ └────────────┘ └─────────────┘ └──────────┘ └──────────┘

📁 Location: /src/main/java/com/smartparking/repository/
└─ All repositories extend JpaRepository (INHERITANCE)
└─ Spring generates implementation at runtime (POLYMORPHISM)


╔══════════════════════════════════════════════════════════════════╗
║              ABSTRACTION + POLYMORPHISM                           ║
║         (Service Layer - Interface + Implementation)             ║
╚══════════════════════════════════════════════════════════════════╝

┌────────────────────────────────────────────────────────────────┐
│                     <<interface>>                               │
│                     ParkingService                              │
│                  (Abstract Contract)                            │
├────────────────────────────────────────────────────────────────┤
│ + parkVehicle(request: VehicleEntryRequest): VehicleResponse   │
│ + exitVehicle(registration: String): VehicleResponse           │
│ + getVehicleByRegistration(registration: String): VehicleResponse│
└──────────────────────┬─────────────────────────────────────────┘
                       │ implements (POLYMORPHISM)
                       │
                       ▼
┌────────────────────────────────────────────────────────────────┐
│                   ParkingServiceImpl                            │
│               (Concrete Implementation)                         │
├────────────────────────────────────────────────────────────────┤
│ - vehicleRepository: VehicleRepository                          │
│ - parkingSlotRepository: ParkingSlotRepository                  │
│ - pricingStrategy: PricingStrategy   ← DEPENDENCY ON ABSTRACTION│
├────────────────────────────────────────────────────────────────┤
│ + @Transactional parkVehicle(...)                              │
│   └─ Implements abstract method with concrete logic            │
│                                                                 │
│ + @Transactional exitVehicle(...)                              │
│   └─ Uses pricingStrategy.calculatePrice() ← POLYMORPHISM      │
│                                                                 │
│ + getVehicleByRegistration(...)                                │
│   └─ Implements abstract method                                │
│                                                                 │
│ - mapVehicleTypeToSlotType(...): SlotType                      │
│   └─ Private helper method (ENCAPSULATION)                     │
└────────────────────────────────────────────────────────────────┘

📁 Interface Location: /src/main/java/com/smartparking/service/ParkingService.java
📁 Implementation Location: /src/main/java/com/smartparking/service/impl/ParkingServiceImpl.java

🎯 OOP Concepts:
   - ABSTRACTION: Interface defines contract
   - POLYMORPHISM: Controller uses interface, Spring injects implementation
   - ENCAPSULATION: Private helper methods


┌────────────────────────────────────────────────────────────────┐
│                     <<interface>>                               │
│                     EmployeeService                             │
├────────────────────────────────────────────────────────────────┤
│ + createEmployee(request: EmployeeRequest): EmployeeResponse   │
│ + getEmployeeById(empId: String): EmployeeResponse             │
│ + getEmployeeByEmail(email: String): EmployeeResponse          │
│ + getAllEmployees(): List<EmployeeResponse>                    │
│ + updateEmployee(empId: String, request: EmployeeRequest)      │
│ + deleteEmployee(empId: String): void                          │
└──────────────────────┬─────────────────────────────────────────┘
                       │ implements
                       ▼
┌────────────────────────────────────────────────────────────────┐
│                   EmployeeServiceImpl                           │
├────────────────────────────────────────────────────────────────┤
│ - employeeRepository: EmployeeRepository                        │
├────────────────────────────────────────────────────────────────┤
│ + All methods implement the interface                          │
│ - mapToResponse(Employee): EmployeeResponse ← ENCAPSULATION    │
└────────────────────────────────────────────────────────────────┘

📁 /src/main/java/com/smartparking/service/EmployeeService.java
📁 /src/main/java/com/smartparking/service/impl/EmployeeServiceImpl.java


┌────────────────────────────────────────────────────────────────┐
│                     <<interface>>                               │
│                     ParkingLotService                           │
├────────────────────────────────────────────────────────────────┤
│ + createParkingLot(...): ParkingLotResponse                    │
│ + getParkingLotById(...): ParkingLotResponse                   │
│ + getAllParkingLots(): List<ParkingLotResponse>                │
│ + addFloor(...): FloorResponse                                 │
│ + getFloorById(...): FloorResponse                             │
│ + getFloorsByParkingLotId(...): List<FloorResponse>            │
└──────────────────────┬─────────────────────────────────────────┘
                       │ implements
                       ▼
┌────────────────────────────────────────────────────────────────┐
│                   ParkingLotServiceImpl                         │
├────────────────────────────────────────────────────────────────┤
│ - parkingLotRepository: ParkingLotRepository                    │
│ - floorRepository: FloorRepository                              │
│ - parkingSlotRepository: ParkingSlotRepository                  │
├────────────────────────────────────────────────────────────────┤
│ + All methods implement the interface                          │
│ - Multiple private helper methods (ENCAPSULATION)              │
└────────────────────────────────────────────────────────────────┘

📁 /src/main/java/com/smartparking/service/ParkingLotService.java
📁 /src/main/java/com/smartparking/service/impl/ParkingLotServiceImpl.java


┌────────────────────────────────────────────────────────────────┐
│                     <<interface>>                               │
│                     AuthService                                 │
├────────────────────────────────────────────────────────────────┤
│ + getUserInfo(uid: String): UserInfoResponse                   │
│ + getCurrentUser(): UserInfoResponse                           │
└──────────────────────┬─────────────────────────────────────────┘
                       │ implements
                       ▼
┌────────────────────────────────────────────────────────────────┐
│                   AuthServiceImpl                               │
├────────────────────────────────────────────────────────────────┤
│ - firebaseAuth: FirebaseAuth                                   │
├────────────────────────────────────────────────────────────────┤
│ + getUserInfo(...): UserInfoResponse                           │
│ + getCurrentUser(): UserInfoResponse                           │
│ - mapToUserInfo(UserRecord): UserInfoResponse ← ENCAPSULATION  │
└────────────────────────────────────────────────────────────────┘

📁 /src/main/java/com/smartparking/service/AuthService.java
📁 /src/main/java/com/smartparking/service/impl/AuthServiceImpl.java


╔══════════════════════════════════════════════════════════════════╗
║              STRATEGY PATTERN (Advanced Polymorphism)            ║
╚══════════════════════════════════════════════════════════════════╝

┌────────────────────────────────────────────────────────────────┐
│                     <<interface>>                               │
│                     PricingStrategy                             │
│                  (Strategy Interface)                           │
├────────────────────────────────────────────────────────────────┤
│ + calculatePrice(vehicleType: VehicleType,                     │
│                  duration: Duration): Double                   │
└──────────────────────┬─────────────────────────────────────────┘
                       │ implements (POLYMORPHISM)
                       │
                       ▼
┌────────────────────────────────────────────────────────────────┐
│                   DefaultPricingStrategy                        │
│             (Concrete Strategy Implementation)                  │
├────────────────────────────────────────────────────────────────┤
│ - MINIMUM_CHARGE: Double = 5.0                                 │
│ - MINIMUM_DURATION_MINUTES: Long = 30                          │
│ - RATE_TWO_WHEELER: Double = 10.0                              │
│ - RATE_FOUR_WHEELER: Double = 20.0                             │
│ - RATE_HEAVY_VEHICLE: Double = 40.0                            │
├────────────────────────────────────────────────────────────────┤
│ + calculatePrice(vehicleType, duration): Double                │
│   ├─ Uses SWITCH on VehicleType (POLYMORPHIC BEHAVIOR)        │
│   ├─ TWO_WHEELER → ₹10/hour                                    │
│   ├─ FOUR_WHEELER → ₹20/hour                                   │
│   └─ HEAVY_VEHICLE → ₹40/hour                                  │
│                                                                 │
│ - getHourlyRate(vehicleType): Double                           │
│   └─ Private method (ENCAPSULATION)                            │
└────────────────────────────────────────────────────────────────┘

📁 Interface: /src/main/java/com/smartparking/service/strategy/PricingStrategy.java
📁 Implementation: /src/main/java/com/smartparking/service/strategy/DefaultPricingStrategy.java

🎯 Strategy Pattern Benefits:
   - Can add new pricing strategies (e.g., WeekendPricingStrategy)
   - Without modifying ParkingServiceImpl
   - Just create new class implementing PricingStrategy


Future Strategy (Example):
┌────────────────────────────────────────────────────────────────┐
│                   WeekendPricingStrategy                        │
│             (Alternative Strategy - Not Implemented)            │
├────────────────────────────────────────────────────────────────┤
│ + calculatePrice(vehicleType, duration): Double                │
│   └─ Applies 50% discount on weekends                          │
└────────────────────────────────────────────────────────────────┘


╔══════════════════════════════════════════════════════════════════╗
║                 INHERITANCE (Class Extension)                     ║
╚══════════════════════════════════════════════════════════════════╝

┌────────────────────────────────────────────────────────────────┐
│                   OncePerRequestFilter                          │
│              (Spring Framework Base Class)                      │
├────────────────────────────────────────────────────────────────┤
│ # doFilterInternal(request, response, filterChain): void       │
│   └─ Abstract method (MUST be overridden)                      │
└──────────────────────┬─────────────────────────────────────────┘
                       │ extends (INHERITANCE)
                       │
                       ▼
┌────────────────────────────────────────────────────────────────┐
│            FirebaseAuthenticationFilter                         │
│          (Concrete Implementation - Child Class)                │
├────────────────────────────────────────────────────────────────┤
│ + doFilterInternal(request, response, filterChain): void       │
│   ├─ Overrides parent abstract method                          │
│   ├─ Extracts Firebase token from Authorization header         │
│   ├─ Verifies token with Firebase Admin SDK                    │
│   ├─ Creates Spring Security Authentication                    │
│   └─ Sets SecurityContext                                      │
│                                                                 │
│ - extractToken(request): String                                │
│   └─ Private method (ENCAPSULATION)                            │
└────────────────────────────────────────────────────────────────┘

📁 Location: /src/main/java/com/smartparking/security/FirebaseAuthenticationFilter.java

🎯 Inheritance Concepts:
   - EXTENDS OncePerRequestFilter (inherits Spring's filter lifecycle)
   - OVERRIDES doFilterInternal() (provides custom authentication logic)
   - Inherits Spring's filter chain integration


╔══════════════════════════════════════════════════════════════════╗
║              ENTITY RELATIONSHIPS (Composition & Association)     ║
╚══════════════════════════════════════════════════════════════════╝

┌────────────────────────────────────────────────────────────────┐
│                        ParkingLot                               │
│                    (Aggregate Root)                             │
├────────────────────────────────────────────────────────────────┤
│ - parkingLotId: String                                          │
│ - name: String                                                  │
│ - address: String                                               │
│ - totalFloors: Integer                                          │
│ - floors: List<Floor>  ◄── COMPOSITION (HAS-A relationship)    │
│ - createdAt: LocalDateTime                                      │
│ - updatedAt: LocalDateTime                                      │
├────────────────────────────────────────────────────────────────┤
│ # onCreate(): void                                              │
│ # onUpdate(): void                                              │
└────────────────┬───────────────────────────────────────────────┘
                 │ 1:N (One-to-Many)
                 │ @OneToMany(cascade = CascadeType.ALL)
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│                        Floor                                    │
│                  (Child Entity)                                 │
├────────────────────────────────────────────────────────────────┤
│ - floorId: String                                               │
│ - floorNo: Integer                                              │
│ - totalSlots: Integer                                           │
│ - allottedSlots: Integer                                        │
│ - parkingLot: ParkingLot  ◄── ASSOCIATION (BELONGS-TO)         │
│ - slots: List<ParkingSlot>  ◄── COMPOSITION                    │
├────────────────────────────────────────────────────────────────┤
│ + incrementAllottedSlots(): void                                │
│ + decrementAllottedSlots(): void                                │
└────────────────┬───────────────────────────────────────────────┘
                 │ 1:N (One-to-Many)
                 │ @OneToMany(cascade = CascadeType.ALL)
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│                      ParkingSlot                                │
│                   (Leaf Entity)                                 │
├────────────────────────────────────────────────────────────────┤
│ - slotId: String                                                │
│ - slotStatus: SlotStatus (enum)                                 │
│ - slotType: SlotType (enum)                                     │
│ - floor: Floor  ◄── ASSOCIATION (BELONGS-TO)                   │
│ - currentVehicle: Vehicle  ◄── ASSOCIATION (1:1)               │
└────────────────┬───────────────────────────────────────────────┘
                 │ 1:1 (One-to-One)
                 │ @OneToOne(mappedBy = "assignedSlot")
                 │
                 ▼
┌────────────────────────────────────────────────────────────────┐
│                        Vehicle                                  │
│                  (Independent Entity)                           │
├────────────────────────────────────────────────────────────────┤
│ - vehicleId: String                                             │
│ - vehicleType: VehicleType (enum)                               │
│ - vehicleRegistration: String                                   │
│ - timeIn: LocalDateTime                                         │
│ - timeOut: LocalDateTime                                        │
│ - status: VehicleStatus (enum)                                  │
│ - billAmt: Double                                               │
│ - assignedSlot: ParkingSlot  ◄── ASSOCIATION                   │
├────────────────────────────────────────────────────────────────┤
│ # onCreate(): void                                              │
└────────────────────────────────────────────────────────────────┘

📁 All Entities: /src/main/java/com/smartparking/entity/

🎯 Relationship Types:
   - COMPOSITION: ParkingLot HAS-A List<Floor>, Floor HAS-A List<ParkingSlot>
   - ASSOCIATION: Floor BELONGS-TO ParkingLot, Slot BELONGS-TO Floor
   - BIDIRECTIONAL: ParkingSlot ↔ Vehicle (both know about each other)


┌────────────────────────────────────────────────────────────────┐
│                        Employee                                 │
│                 (Standalone Entity)                             │
├────────────────────────────────────────────────────────────────┤
│ - empId: String                                                 │
│ - name: String                                                  │
│ - email: String (UNIQUE)                                        │
│ - phNo: String                                                  │
│ - dob: LocalDate                                                │
│ - gender: Gender (enum)                                         │
│ - photo: String                                                 │
│ - address: String                                               │
│ - roles: String                                                 │
│ - createdAt: LocalDateTime                                      │
│ - updatedAt: LocalDateTime                                      │
├────────────────────────────────────────────────────────────────┤
│ # onCreate(): void                                              │
│ # onUpdate(): void                                              │
└────────────────────────────────────────────────────────────────┘

📁 Location: /src/main/java/com/smartparking/entity/Employee.java


╔══════════════════════════════════════════════════════════════════╗
║                    ENCAPSULATION                                  ║
║         (Private fields + Public methods + Lombok)               ║
╚══════════════════════════════════════════════════════════════════╝

ALL ENTITIES use ENCAPSULATION via Lombok annotations:

@Data                    ← Generates getters/setters automatically
@NoArgsConstructor      ← Generates no-arg constructor
@AllArgsConstructor     ← Generates all-args constructor
@Builder                ← Generates builder pattern

Example:
┌────────────────────────────────────────────────────────────────┐
│ @Entity                                                         │
│ @Data  ◄── Lombok generates: getters, setters, equals,         │
│            hashCode, toString                                   │
│ @NoArgsConstructor                                              │
│ @AllArgsConstructor                                             │
│ @Builder                                                        │
│ public class Employee {                                         │
│     @Id                                                         │
│     private String empId;  ◄── ENCAPSULATED (private)           │
│                                                                 │
│     @Column(nullable = false, unique = true)                   │
│     private String email;  ◄── ENCAPSULATED                     │
│                                                                 │
│     // Lombok generates:                                       │
│     // + public String getEmpId() { return empId; }            │
│     // + public void setEmpId(String empId) {                  │
│     //     this.empId = empId;                                 │
│     //   }                                                      │
│     // + Similar for all fields                                │
│ }                                                               │
└────────────────────────────────────────────────────────────────┘

🎯 Encapsulation Benefits:
   - Private fields cannot be accessed directly
   - Public methods control access (getters/setters)
   - Validation can be added in setters
   - Implementation can change without affecting clients


╔══════════════════════════════════════════════════════════════════╗
║                  POLYMORPHISM IN ACTION                           ║
╚══════════════════════════════════════════════════════════════════╝

Example 1: Controller → Service Interface (Runtime Polymorphism)
┌────────────────────────────────────────────────────────────────┐
│ @RestController                                                 │
│ @RequiredArgsConstructor                                        │
│ public class ParkingController {                                │
│                                                                 │
│     private final ParkingService parkingService;                │
│     //              ▲                                           │
│     //              │                                           │
│     //          INTERFACE (Abstract)                            │
│     //              │                                           │
│     //       At runtime, Spring injects:                        │
│     //       ParkingServiceImpl (Concrete)                      │
│     //              │                                           │
│     //       This is POLYMORPHISM!                              │
│                                                                 │
│     @PostMapping("/entry")                                      │
│     public ResponseEntity<ApiResponse<VehicleResponse>>         │
│         parkVehicle(@Valid @RequestBody VehicleEntryRequest r) {│
│                                                                 │
│         // Calls interface method, but executes implementation  │
│         VehicleResponse response = parkingService.parkVehicle(r);│
│         //                         ▲                            │
│         //                         │                            │
│         //                  POLYMORPHISM IN ACTION              │
│         //                         │                            │
│         //         Which parkVehicle() is called?               │
│         //         → ParkingServiceImpl.parkVehicle()           │
│         //         (Determined at RUNTIME by Spring)            │
│                                                                 │
│         return ResponseEntity.status(201)                       │
│                 .body(ApiResponse.success(response));           │
│     }                                                           │
│ }                                                               │
└────────────────────────────────────────────────────────────────┘


Example 2: Strategy Pattern (Pricing Algorithm)
┌────────────────────────────────────────────────────────────────┐
│ @Service                                                        │
│ @RequiredArgsConstructor                                        │
│ public class ParkingServiceImpl implements ParkingService {     │
│                                                                 │
│     private final PricingStrategy pricingStrategy;              │
│     //              ▲                                           │
│     //              │                                           │
│     //          INTERFACE (Can be swapped!)                     │
│     //              │                                           │
│     //       Current: DefaultPricingStrategy                    │
│     //       Future: WeekendPricingStrategy, PeakHourPricing    │
│                                                                 │
│     public VehicleResponse exitVehicle(String registration) {   │
│         // ... find vehicle, calculate duration ...             │
│                                                                 │
│         // Calculate bill using strategy                        │
│         Double billAmt = pricingStrategy.calculatePrice(        │
│             vehicle.getVehicleType(),                           │
│             duration                                            │
│         );                                                      │
│         //              ▲                                       │
│         //              │                                       │
│         //        POLYMORPHISM!                                 │
│         //              │                                       │
│         //   Which calculatePrice() is called?                  │
│         //   → DefaultPricingStrategy.calculatePrice()          │
│         //   (Can be changed by injecting different strategy)   │
│                                                                 │
│         vehicle.setBillAmt(billAmt);                            │
│         // ...                                                  │
│     }                                                           │
│ }                                                               │
└────────────────────────────────────────────────────────────────┘


Example 3: Repository (Spring Data JPA Polymorphism)
┌────────────────────────────────────────────────────────────────┐
│ public interface VehicleRepository                              │
│         extends JpaRepository<Vehicle, String> {                │
│                 ▲                                               │
│                 │                                               │
│           Spring generates implementation                       │
│           at RUNTIME (Proxy pattern)                            │
│                 │                                               │
│     Optional<Vehicle> findByVehicleRegistration(String reg);    │
│     //                ▲                                         │
│     //                │                                         │
│     //        Spring generates SQL:                             │
│     //        SELECT * FROM vehicles                            │
│     //        WHERE vehicle_registration = ?                    │
│     //                │                                         │
│     //          METHOD NAME PARSING                             │
│     //          (Convention over Configuration)                 │
│ }                                                               │
│                                                                 │
│ Usage in Service:                                               │
│ Optional<Vehicle> vehicle =                                     │
│     vehicleRepository.findByVehicleRegistration(registration);  │
│     //                ▲                                         │
│     //                │                                         │
│     //   Calls Spring-generated implementation                  │
│     //   (POLYMORPHISM - interface → proxy implementation)      │
└────────────────────────────────────────────────────────────────┘


╔══════════════════════════════════════════════════════════════════╗
║            COMPLETE OOP CONCEPTS MAPPING                          ║
╚══════════════════════════════════════════════════════════════════╝

┌──────────────────┬─────────────────────────────────────────────┐
│  OOP CONCEPT     │  IMPLEMENTATION IN PROJECT                  │
├──────────────────┼─────────────────────────────────────────────┤
│                  │                                             │
│  ABSTRACTION     │ • Service Interfaces (AuthService,          │
│                  │   EmployeeService, ParkingService, etc.)    │
│                  │   📁 /service/*.java                        │
│                  │                                             │
│                  │ • Repository Interfaces (JpaRepository)     │
│                  │   📁 /repository/*.java                     │
│                  │                                             │
│                  │ • Strategy Interface (PricingStrategy)      │
│                  │   📁 /service/strategy/PricingStrategy.java │
│                  │                                             │
├──────────────────┼─────────────────────────────────────────────┤
│                  │                                             │
│  INHERITANCE     │ • All Repositories extend JpaRepository     │
│                  │   📁 /repository/*.java                     │
│                  │                                             │
│                  │ • FirebaseAuthenticationFilter extends      │
│                  │   OncePerRequestFilter (Spring)             │
│                  │   📁 /security/FirebaseAuthentication       │
│                  │      Filter.java                            │
│                  │                                             │
├──────────────────┼─────────────────────────────────────────────┤
│                  │                                             │
│  POLYMORPHISM    │ • Service Implementations (at runtime)      │
│                  │   📁 /service/impl/*ServiceImpl.java        │
│                  │                                             │
│                  │ • Strategy Pattern (PricingStrategy)        │
│                  │   📁 /service/strategy/                     │
│                  │      DefaultPricingStrategy.java            │
│                  │                                             │
│                  │ • Repository proxy generation (Spring)      │
│                  │   📁 /repository/*.java                     │
│                  │                                             │
│                  │ • Method Overriding                         │
│                  │   - doFilterInternal() in Filter            │
│                  │   - All service interface methods           │
│                  │                                             │
├──────────────────┼─────────────────────────────────────────────┤
│                  │                                             │
│  ENCAPSULATION   │ • All entities with private fields          │
│                  │   📁 /entity/*.java                         │
│                  │   • @Data (Lombok) generates getters/setters│
│                  │   • Private helper methods in services      │
│                  │   • Package-private visibility              │
│                  │                                             │
├──────────────────┼─────────────────────────────────────────────┤
│                  │                                             │
│  COMPOSITION     │ • ParkingLot HAS-A List<Floor>              │
│                  │ • Floor HAS-A List<ParkingSlot>             │
│                  │   📁 /entity/ParkingLot.java                │
│                  │   📁 /entity/Floor.java                     │
│                  │                                             │
├──────────────────┼─────────────────────────────────────────────┤
│                  │                                             │
│  ASSOCIATION     │ • Floor BELONGS-TO ParkingLot               │
│                  │ • ParkingSlot BELONGS-TO Floor              │
│                  │ • Vehicle ASSIGNED-TO ParkingSlot           │
│                  │   📁 /entity/*.java (JPA @ManyToOne,        │
│                  │      @OneToOne relationships)               │
│                  │                                             │
└──────────────────┴─────────────────────────────────────────────┘


╔══════════════════════════════════════════════════════════════════╗
║              FOLDER STRUCTURE WITH OOP CONCEPTS                   ║
╚══════════════════════════════════════════════════════════════════╝

src/main/java/com/smartparking/
│
├── 📁 config/                        [Configuration]
│   ├── SecurityConfig.java
│   └── FirebaseConfig.java
│
├── 📁 controller/                    [Presentation - Uses Abstraction]
│   ├── AuthController.java           ├─ Depends on AuthService (interface)
│   ├── EmployeeController.java       ├─ Depends on EmployeeService (interface)
│   ├── ParkingController.java        ├─ Depends on ParkingService (interface)
│   ├── ParkingLotController.java     ├─ Depends on ParkingLotService (interface)
│   └── HealthController.java         └─ POLYMORPHISM at runtime
│
├── 📁 service/                       [ABSTRACTION - Interfaces]
│   ├── AuthService.java              ┐
│   ├── EmployeeService.java          │ Interfaces define contracts
│   ├── ParkingService.java           │ (WHAT, not HOW)
│   ├── ParkingLotService.java        ┘
│   │
│   ├── 📁 impl/                      [POLYMORPHISM - Implementations]
│   │   ├── AuthServiceImpl.java     ┐
│   │   ├── EmployeeServiceImpl.java │ Concrete implementations
│   │   ├── ParkingServiceImpl.java  │ (HOW - business logic)
│   │   └── ParkingLotServiceImpl.java┘
│   │
│   └── 📁 strategy/                  [STRATEGY PATTERN]
│       ├── PricingStrategy.java     ← Interface (ABSTRACTION)
│       └── DefaultPricingStrategy.java ← Implementation (POLYMORPHISM)
│
├── 📁 repository/                    [ABSTRACTION + INHERITANCE]
│   ├── EmployeeRepository.java       ┐
│   ├── VehicleRepository.java        │ All extend JpaRepository
│   ├── ParkingSlotRepository.java    │ (INHERITANCE)
│   ├── FloorRepository.java          │ Spring generates implementations
│   └── ParkingLotRepository.java     ┘ (POLYMORPHISM)
│
├── 📁 entity/                        [DOMAIN MODEL - Encapsulation]
│   ├── Employee.java                 ┐
│   ├── Vehicle.java                  │ Private fields + public methods
│   ├── ParkingSlot.java              │ (ENCAPSULATION via Lombok)
│   ├── Floor.java                    │ Relationships: COMPOSITION,
│   └── ParkingLot.java               ┘ ASSOCIATION
│
├── 📁 security/                      [INHERITANCE]
│   └── FirebaseAuthenticationFilter.java ← extends OncePerRequestFilter
│
├── 📁 dto/                           [Data Transfer Objects]
│   ├── 📁 request/                   [Input DTOs]
│   │   ├── EmployeeRequest.java
│   │   ├── VehicleEntryRequest.java
│   │   ├── ParkingLotRequest.java
│   │   └── FloorRequest.java
│   │
│   └── 📁 response/                  [Output DTOs]
│       ├── ApiResponse.java          ← Generic wrapper
│       ├── EmployeeResponse.java
│       ├── VehicleResponse.java
│       ├── ParkingLotResponse.java
│       ├── FloorResponse.java
│       ├── ParkingSlotResponse.java
│       └── UserInfoResponse.java
│
├── 📁 enums/                         [Type Safety]
│   ├── Gender.java
│   ├── SlotType.java
│   ├── SlotStatus.java
│   ├── VehicleType.java
│   └── VehicleStatus.java
│
├── 📁 exception/                     [Error Handling]
│   ├── GlobalExceptionHandler.java
│   ├── ResourceNotFoundException.java
│   ├── DuplicateResourceException.java
│   ├── InvalidOperationException.java
│   └── NoAvailableSlotException.java
│
└── SmartParkingApplication.java     [Entry Point]


╔══════════════════════════════════════════════════════════════════╗
║                   DESIGN PATTERNS USED                            ║
╚══════════════════════════════════════════════════════════════════╝

1. STRATEGY PATTERN
   📁 /service/strategy/PricingStrategy.java
   📁 /service/strategy/DefaultPricingStrategy.java
   └─ Allows swapping pricing algorithms at runtime

2. REPOSITORY PATTERN
   📁 /repository/*.java
   └─ Abstracts data access layer

3. DEPENDENCY INJECTION
   └─ All classes use constructor injection via @RequiredArgsConstructor

4. DTO PATTERN
   📁 /dto/request/*.java
   📁 /dto/response/*.java
   └─ Separates API contracts from domain models

5. BUILDER PATTERN
   └─ All entities use @Builder (Lombok)

6. TEMPLATE METHOD PATTERN
   📁 /security/FirebaseAuthenticationFilter.java
   └─ Extends OncePerRequestFilter (Spring)

7. FACADE PATTERN
   └─ Service layer facades complex operations


╔══════════════════════════════════════════════════════════════════╗
║                      SUMMARY                                      ║
╚══════════════════════════════════════════════════════════════════╝

OOP Concept         | Count | Primary Location
--------------------|-------|----------------------------------
Interfaces          |  9    | /service/*.java, /repository/*.java
Implementations     |  5    | /service/impl/*.java, /strategy/*.java
Inheritance         |  6    | All repositories + Filter
Polymorphism        | All   | Services, Repositories, Strategies
Encapsulation       | All   | All entities (via Lombok @Data)
Composition         |  2    | ParkingLot→Floor, Floor→ParkingSlot
Association         |  4    | All entity relationships

Total Classes:      ~50
Total Interfaces:   ~10
Lines of Code:      ~3000

✅ Clean separation of concerns
✅ Dependency inversion principle applied
✅ Open/Closed principle (Strategy pattern)
✅ Single responsibility principle
✅ Interface segregation principle

This architecture allows:
- Easy unit testing (mock interfaces)
- Flexible implementations (swap strategies)
- Maintainable codebase (clear structure)
- Extensible design (add new features without breaking existing)